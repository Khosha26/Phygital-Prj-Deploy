# Universe cosmos home-bg — manual steps to finish

Generated by Higgsfield (nano_banana_2 / Nano Banana Pro), image-to-image from
`/Users/mi1k/universe-work/assets/renders/client/ext-hero-twilight.jpg`.

## Done (stills, 2752x1536 PNG, 16:9)
- cosmos-hero-v1.png  — towers grounded + thin gold orbital ring + soft nebula (cleanest/most grounded)
- cosmos-hero-v2.png  — towers floating + concentric Saturn-style gold orrery rings (most conceptual)
- cosmos-hero-v3.png  — twin towers inside a full champagne SPIRAL GALAXY w/ glowing core (RECOMMENDED — best matches brief)

## NOT done — two blockers hit (see report)
1. JPG conversion / canvas resize to 2560x1600 — Bash WRITE (ffmpeg) was permission-denied.
2. Animated loop (cosmos-loop.mp4) — Higgsfield video tool permission-denied + CLI auth expired.

## To finish manually

### 1) Make the kiosk JPGs (canvas 2560x1600, 16:10 from 16:9 source — scale to width then center-crop height)
```
cd /Users/mi1k/universe-work/assets/home-bg/
for v in v1 v2 v3; do
  ffmpeg -y -i cosmos-hero-$v.png -vf "scale=2560:-1,crop=2560:1600" -q:v 3 cosmos-hero-$v.jpg
done
cp cosmos-hero-v3.jpg cosmos-hero.jpg   # v3 = recommended primary
```

### 2) Animate the recommended still to a seamless loop
Higgsfield CLI (after `hf auth login` in a browser):
```
higgsfield generate create seedance_2_0 \
  --aspect_ratio 16:9 --resolution 1080p --mode std --genre epic --duration 10 \
  --start-image /Users/mi1k/universe-work/assets/home-bg/cosmos-hero-v3.png \
  --end-image   /Users/mi1k/universe-work/assets/home-bg/cosmos-hero-v3.png \
  --prompt "Seamless living loop, extremely slow and subtle, camera locked static (no camera move, no zoom). The twin towers stay perfectly still and sharp. The surrounding champagne-gold spiral galaxy slowly drifts and rotates, nebula clouds gently billow, the bright galactic core between the towers softly pulses and breathes with light, golden cosmic dust slowly floats, hundreds of background stars twinkle and shimmer, the faint orbital ring slowly arcs. Warm gold light, deep black space. Ends exactly where it begins for a perfect seamless loop. Calm, hypnotic, premium, cinematic, real-estate grade. No people, no text changes, no flicker." \
  --wait
```
Using start_image == end_image asks the model to return to the same frame = loopable.

### 3) Download + optimize the MP4 for the offline kiosk (<15MB)
```
curl -sS "<RESULT_URL>" -o cosmos-loop-raw.mp4
# Compress to H.264, no audio, kiosk-friendly; trim to clean loop length if needed
ffmpeg -y -i cosmos-loop-raw.mp4 -an -c:v libx264 -crf 26 -preset slow \
  -pix_fmt yuv420p -movflags +faststart cosmos-loop.mp4
# For an even tighter cross-fade loop you can do a reverse-append (ping-pong):
# ffmpeg -i cosmos-loop.mp4 -filter_complex "[0]reverse[r];[0][r]concat=n=2:v=1:a=0" pingpong.mp4
```
